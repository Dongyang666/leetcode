public class FastPowerOperation {
    // 10
    /*public static long pow(long res, long pow) {
        long tmp = 1;
        long i = res;
        while (pow != 0) {
            tmp = (pow & 1) == 0 ? tmp : tmp * i;
            i *= i;
            pow >>= 1;
        }
        return tmp;
    }*/
    public static void main(String[] args) {


        System.out.println();

    }
}
